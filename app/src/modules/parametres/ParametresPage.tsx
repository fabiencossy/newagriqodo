import { StubPage } from '../_shared/StubPage';

export default function ParametresPage() {
  return (
    <StubPage
      title="Paramètres"
      subtitle="Exploitation, utilisateurs, intégrations"
      description="Configuration globale : exploitation, mapping employés Odoo, master data, tile server."
    />
  );
}
